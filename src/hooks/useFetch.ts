import axios from 'axios'
import { useState, useEffect } from 'react'

interface IProps {
	query: string
	handleSubmit: () => any
}

const useFetch = ({ query, handleSubmit }: IProps) => {
	return null
}

export default useFetch
